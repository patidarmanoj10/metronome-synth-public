These contracts were forked from version 4.3.2

# Changelog

- Replaced `ERC20.sol` file for `v4.8.0` because LZ OFT contracts need `_spendAllowance` not available before.
- Added the `utils/TransientSlot.sol` file that was extracted from `utils/StorageSlot.sol` (`v5.1`)
