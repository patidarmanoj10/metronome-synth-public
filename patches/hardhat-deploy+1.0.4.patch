diff --git a/node_modules/hardhat-deploy/dist/src/helpers.js b/node_modules/hardhat-deploy/dist/src/helpers.js
index 0903e5e..d8cbb82 100644
--- a/node_modules/hardhat-deploy/dist/src/helpers.js
+++ b/node_modules/hardhat-deploy/dist/src/helpers.js
@@ -978,6 +978,9 @@ Note that in this case, the contract deployment will not behave the same if depl
                 if (!upgradeMethod) {
                     throw new Error(`No upgrade method found, cannot make upgrades`);
                 }
+                // Wait for the RPC to reflect the implementation-deploy nonce before
+                // broadcasting the upgrade tx (avoids same-nonce errors on Base).
+                await new Promise((r) => setTimeout(r, (Number(process.env.WAIT_SECONDS_BETWEEN_TXS) || 0) * 1000));
                 let executeReceipt;
                 if (proxyAdminName) {
                     if (oldProxy) {
diff --git a/node_modules/hardhat-deploy/src/DeploymentsManager.ts b/node_modules/hardhat-deploy/src/DeploymentsManager.ts
index bcfd957..03e94ba 100644
--- a/node_modules/hardhat-deploy/src/DeploymentsManager.ts
+++ b/node_modules/hardhat-deploy/src/DeploymentsManager.ts
@@ -1209,6 +1209,7 @@ export class DeploymentsManager {
           log(`executing  ${deployScript.filePath}`);
           let result;
           try {
+            await new Promise((r) => setTimeout(r, (Number(process.env.WAIT_SECONDS_BETWEEN_TXS) || 0) * 1000))
             result = await deployScript.func(this.env);
           } catch (e) {
             // console.error("execution failed", e);
diff --git a/node_modules/hardhat-deploy/src/helpers.ts b/node_modules/hardhat-deploy/src/helpers.ts
index ecf408e..84e3e69 100644
--- a/node_modules/hardhat-deploy/src/helpers.ts
+++ b/node_modules/hardhat-deploy/src/helpers.ts
@@ -1525,6 +1525,8 @@ Note that in this case, the contract deployment will not behave the same if depl
           throw new Error(`No upgrade method found, cannot make upgrades`);
         }
 
+        await new Promise((r) => setTimeout(r, (Number(process.env.WAIT_SECONDS_BETWEEN_TXS) || 0) * 1000))
+
         let executeReceipt;
         if (proxyAdminName) {
           if (oldProxy) {
